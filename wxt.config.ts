import { defineConfig } from 'wxt';

export default defineConfig({
  manifest: {
    name: 'Novo DOOL — Protótipo',
    description: 'Camada experimental, reversível e isolada para modernização da interface do DOOL.',
    version: '0.1.1',
    permissions: ['storage'],
  },
});
